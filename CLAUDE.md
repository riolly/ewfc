Always use [SolidJs v2](v2.solidjs.com) and /unslop skill.

Issue tracker: GitHub issues via the `gh` CLI (repo `riolly/ewfc`). The wayfinder map is issue #1; tickets are its sub-issues, typed by `wayfinder:*` label and gated by native issue dependencies.
